import DCFProfileToggle from '@dcf/js/components/dcf-profile-toggle.js';

export default class UNLProfileToggle extends DCFProfileToggle {
    constructor(toggleProfileElement, options={}) {
        super(toggleProfileElement, options);
    }
}
