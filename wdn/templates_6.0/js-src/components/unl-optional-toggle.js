import DCFOptionalToggle from '@dcf/js/components/dcf-optional-toggle.js';

export default class UNLOptionalToggle extends DCFOptionalToggle {
    constructor(toggleOptionalElement, options={}) {
        super(toggleOptionalElement, options);
    }
}
